//#FILENAME:src/service/{{controller-name this}}RouteService.tsx:create-only

import type { Request, Response } from 'express';
import { ConfigProvider } from "@kapeta/sdk-config";
import {
{{controller-name this}}Routes
} from 'generated:rest/{{controller-name this}}Routes';


/**
 * Creates the {{controller-name this}}RouteService.
 *
 * Class is created inside the function to allow async initialization to happen. The class structure is optional
 * but just makes it easier to organize the code.
 *
 * @param configProvider The configuration provider to use.
 */
export const create{{controller-name this}}RouteService = async (configProvider:ConfigProvider):Promise<{{controller-name this}}Routes> => {

    return {

    {{#methods methods}}

        /**
        * {{comment description}}
        * HTTP: {{./method}} {{expressPath path base=../path}}
        */
        {{method name}}( req, res ): void {
        res.sendError('REST resource method not implemented: "{{method name}}"', 501);
        },

    {{/methods}}
    };
}
