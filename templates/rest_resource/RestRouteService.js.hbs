//#FILENAME:src/service/{{type data.metadata.name}}RouteService.js:create-only

class {{type data.metadata.name}}RouteService {

    {{#methods data.spec.methods}}

        /**
         * {{comment description}}
         * HTTP: {{./method}} {{path}}
         *
{{#eachProperty arguments}}
         * @param {{curly 1}}{{type type}}{{curly}} {{variable propertyId}}
{{/eachProperty}}
         * @return {{curly 1}}Promise<{{returnType responseType}}>{{curly}}
         */
        async {{method methodName}}(
                    {{#arguments arguments}}
                        {{variable argumentName}}
                    {{/arguments}}
                ) {
            throw new Error('REST resource method not implemented: "{{method methodName}}"');
        }

    {{/methods}}
}

module.exports = {{type data.metadata.name}}RouteService;