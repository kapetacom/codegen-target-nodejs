//#FILENAME:src/clients/{{type data.metadata.name}}Client.ts:write-always
//
// GENERATED SOURCE - DO NOT EDIT
//
import { RestClient } from '@kapeta/sdk-rest-client';
{{#eachTypeReference data.spec.methods all=true}}
    import { {{name}} } from "../entities/{{name}}";
{{/eachTypeReference}}

export class {{type data.metadata.name}}Client {
        private readonly client:RestClient;

        constructor() {
            this.client = new RestClient('{{string data.metadata.name}}');
        }

    {{#methods data.spec.methods}}


        /**
         * {{comment description}}
         * HTTP: {{./method}} {{path}}
         */
        {{method methodName}}(
                {{#arguments arguments}}
                    {{variable argumentName}}:{{fieldtype this}}
                {{/arguments}}
                ):Promise<{{returnType responseType}}> {

            return this.client.execute(
                '{{string method}}',
                '{{string path}}',
                [
                    {{#arguments arguments}}
                        {
                            name: '{{string argumentName}}',
                            value: {{variable argumentName}},
                            transport: '{{string transport}}'
                        }
                    {{/arguments}}
                ]
            );
        }

    {{/methods}}
}