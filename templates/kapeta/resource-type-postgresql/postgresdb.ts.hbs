//#FILENAME:src/.generated/data/{{type data.metadata.name}}DB.ts:write-always
//
// GENERATED SOURCE - DO NOT EDIT
//
import { PostgresDB } from '@kapeta/sdk-sqldb-postgresql';
import { Prisma, PrismaClient } from './clients/{{data.metadata.name}}';

export class {{type data.metadata.name}}DB extends PostgresDB<PrismaClient> {
    constructor() {
        super('{{string data.metadata.name}}');
    }

    createClient(opts: Prisma.PrismaClientOptions): PrismaClient {
        return new PrismaClient(opts);
    }
}