//#FILENAME:src/.generated/data/{{type data.metadata.name}}DB.ts:write-always
//
// GENERATED SOURCE - DO NOT EDIT
//
import Config,{ConfigProvider} from "@kapeta/sdk-config";
import { RedisDB as $RedisDB, createRedisClient as $createRedisClient, RedisClient } from '@kapeta/sdk-redis';

/**
 * Create a Redis client for the {{data.metadata.name}} database.
 */
export const create{{type data.metadata.name}}Client = (config?: ConfigProvider):Promise<RedisClient> => {
    return $createRedisClient(config ?? Config.getProvider(), '{{string data.metadata.name}}');
}

/**
 * Use this class to access the {{data.metadata.name}} database.
 *
 * Recommended use is to call the ```create{{type data.metadata.name}}Client``` function to create a client.
 */
export class {{type data.metadata.name}}DB extends $RedisDB {
    constructor() {
        super('{{string data.metadata.name}}');
    }
}