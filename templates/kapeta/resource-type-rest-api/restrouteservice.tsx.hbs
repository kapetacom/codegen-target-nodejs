//#FILENAME:src/service/{{type data.metadata.name}}RouteService.tsx:create-only

import { I{{type data.metadata.name}}RouteService } from '../rest/I{{type data.metadata.name}}RouteService';
{{#eachTypeReference data.spec.methods all=true}}
    import { {{name}} } from "../entities/{{name}}";
{{/eachTypeReference}}

export class {{type data.metadata.name}}RouteService implements I{{type data.metadata.name}}RouteService {

    {{#methods data.spec.methods}}

        /**
        * {{comment description}}
        * HTTP: {{./method}} {{path}}
        */
        {{method methodName}}( {{#arguments arguments}}
                    {{variable argumentName}}:{{fieldtype this}}
                {{/arguments}} ): Promise<{{returnType responseType}}> {
            throw new Error('REST resource method not implemented: "{{method methodName}}"');
        }

    {{/methods}}
}